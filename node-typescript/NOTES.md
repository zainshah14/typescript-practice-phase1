# Phase 1 – Mid-Review Snapshot

## What’s implemented
- TypeScript Sections 1–6, 14, 20 applied to working code
- Express CRUD API (todos) with JSON middleware & error handling
- Jest + ts-jest + SuperTest request tests (health, create, list, update, delete)
- ESLint (ESLint v9 flat config) – clean run
- README with run/test/lint/coverage instructions

## How to run
- Dev: `npm run dev`
- Build + start: `npm run compile && npm start`
- Tests: `npm test`  |  Coverage: `npm run coverage`
- Lint: `npm run lint`